apiVersion: apps/v1
kind: Deployment
metadata:
  name: device-service
  namespace: irrigation-production
  labels:
    app: device-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: device-service
  template:
    metadata:
      labels:
        app: device-service
    spec:
      containers:
      - name: device-service
        image: ${DOCKER_USERNAME}/irrigation-device-service:${VERSION:-latest}
        ports:
        - containerPort: 3006
        env:
        - name: PORT
          value: "3006"
        envFrom:
        - secretRef:
            name: irrigation-secrets
        - configMapRef:
            name: irrigation-config
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3006
          initialDelaySeconds: 30
          periodSeconds: 10
